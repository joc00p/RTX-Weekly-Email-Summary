# User-specific IDE / MSBuild files
*.csproj.user

# .NET build output
bin/
obj/
publish/

# OutlookSearch lives here as its own repo (github.com/joc00p/OutlookSearch)
/OutlookSearch/

# IRCServer lives here as its own repo (github.com/joc00p/IRCServer)
/IRCServer/

# IRCBot lives here as its own repo (github.com/joc00p/IRCBot)
/IRCBot/

# IRCClient lives here as its own repo (github.com/joc00p/IRCClient)
/IRCClient/

# SSHClient lives here as its own repo (github.com/joc00p/SSHClient)
/SSHClient/

# EtherNetTools lives here as its own repo (github.com/joc00p/EtherNetTools)
/EtherNetTools/
